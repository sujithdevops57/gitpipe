pipeline {
    agent {
        label 'node2-pipe2-label'
    }
    stages {
        stage('Maven') {
            steps {
                echo "hello welcome to sujith Maven"
                  sh "mvn --version"
            }
        }
    }
}
