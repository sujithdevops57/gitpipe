pipeline {
    agent {
        label 'jenkins-slave1'
    }
    stages {
        stage('build') {
            steps {
                echo "hello welcome to jenkins"
            
            }
        }
    }
}
